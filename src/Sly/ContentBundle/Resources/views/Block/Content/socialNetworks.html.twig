{% from "SlyContentBundle:Macro:socialShare.html.twig" import facebookButton, twitterButton, facebookShareButton, googlePlusButton, linkedInButton %}

<ul class="share">
    <li class="facebook-button">{{ facebookButton(url('%type%_show' | replace({'%type%': type}), {'slug': item.slug}), item) }}</li>
    <li class="twitter-button">{{ twitterButton(url('%type%_show' | replace({'%type%': type}), {'slug': item.slug}), item) }}</li>
    <li class="googleplus-button">{{ googlePlusButton(url('%type%_show' | replace({'%type%': type}), {'slug': item.slug}), item) }}</li>
    <li class="linkedin-button">{{ linkedInButton(url('%type%_show' | replace({'%type%': type}), {'slug': item.slug}), item) }}</li>
    <li class="facebook-share-button">{{ facebookShareButton(url('%type%_show' | replace({'%type%': type}), {'slug': item.slug}), item) }}</li>
</ul>