<section>
    <h3>{{ 'sidebar.categories.title' | trans }}</h3>
    
    <div class="content">
        <nav>
            {% if category is defined %}
                {% render 'SlyContentBundle:Component:categories' with {'type': 'blog', 'activeCategory': category} %}
            {% else %}
                {% render 'SlyContentBundle:Component:categories' with {'type': 'blog'} %}
            {% endif %}
        </nav>
    </div>
</section>

{% include 'SlyContentBundle:Block:tabSkel1.html.twig' %}