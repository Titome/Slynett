{% spaceless %}
    <ul class="categories {{ mainRoute }}">
        <li class="item all {% if activeCategory is null and noActiveCategory is null %}active{% endif %}">
            <a href="{{ path(mainRoute) }}">
                {{ 'sidebar.categories.all' | trans }}
            </a>
        </li>
        
        {% for cat in categories %}
            {% if (cat.content | length) %}
                {% if activeCategory == cat %}
                    <li class="item active">
                {% else %}
                    <li class="item">
                {% endif %}
                    <a href="{{ path(catRoute, {'slug': cat.slug}) }}" title="{{ cat }}">
                        {{ cat }}
                    </a>
                </li>
            {% endif %}
        {% endfor %}
    </ul>
{% endspaceless %}