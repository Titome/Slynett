<form method="get" action="{{ path('search_redirect') }}" class="search">
    <div>
        <span>L</span>
        <input type="text" name="q" placeholder="{{ 'search.input' | trans }}" value="{% if searchQuery is defined %}{{ searchQuery }}{% endif %}" />
    </div>
</form>